/**
 * Placeholder for optional cron / scheduled-task UI in the main shell.
 * xClaw layout keeps the mount point; extend when a bubble UX is needed.
 */
export default function ConsoleCronBubble() {
  return null;
}
