registry=https://registry.npmjs.org
# Hoist CodeMirror peers used by @agentscope-ai/design so Vite can resolve them
# without duplicating them in package.json.
public-hoist-pattern[]=@codemirror/*
public-hoist-pattern[]=@uiw/*
public-hoist-pattern[]=codemirror
